# 项目说明与风险声明

> 本文档如实说明本项目的开发背景、开发方式与潜在风险。
> **请所有使用者在部署或二次开发前务必阅读本文档。**

---

## 一、项目背景与发展历程

本项目（校园论坛系统）是一个面向校园用户的开源论坛平台，包含服务端
（Node.js + Express + MongoDB）、Web 前端（原生 HTML/CSS/JS）与 Android
客户端（Java + OkHttp）三个部分。

项目并非由传统团队按常规流程开发，其发展历程如下：

| 时间 | 阶段 | 说明 |
|------|------|------|
| **2025 年 5 月** | 雏形形成 | 项目最初版本开始成形 |
| **2025 年 7 月** | 正式版本 | 形成正式可用的版本 |
| **2025 年 10 月前后** | 引入 agent 工具 | 开始使用 agent 工具辅助开发与优化 |
| **至今** | 持续迭代 | 基于 agent 工具持续修复与新增功能 |

## 二、开发方式说明

本项目**核心代码全部由 AI 生成**，开发方式经历了两个阶段：

### 阶段一：纯网页端对话（2025年5月 ~ 2025年10月前）

- 通过网页端 AI 对话，**逐段生成代码**
- 开发者**手动复制代码粘贴到项目文件**，再将报错信息复制回对话中让 AI 修复
- 整个过程**没有使用 agent 工具**，没有自动化测试、没有代码审查工具、
  没有版本控制的纪律性约束
- 这一阶段积累了项目绝大部分的基础代码

### 阶段二：agent 工具辅助（2025年10月后）

- 引入 agent 工具后，对已有代码进行了多轮优化、重构与问题修复
- 但**受当时模型能力的限制**，优化更多集中在"已知问题"上
- 早期（阶段一）积累的代码中，**可能存在大量未知的小问题与安全漏洞**，
  它们可能从未被任何工具或人工发现过

## 三、已实施的安全加固

随着开发的推进，维护者**逐渐意识到安全问题**，并在后期（agent 工具阶段）
打了多轮安全补丁。以下为历史提交中可追溯的部分加固措施（按类别归纳）：

### 1. 凭据与配置安全
- SMTP 邮箱凭据从代码硬编码迁移至 `.env` 环境变量
- JWT 密钥移除硬编码 fallback，未配置时启动即报错，强制使用强密钥

### 2. 认证与授权加固
- 所有写操作路由统一接入身份认证中间件（帖子/收藏/黑名单/通知/举报/
  关注/私信/用户等）
- 控制器写操作一律使用服务端 `req.user.id`（JWT 解析），不再信任客户端
  传入的 userId，防身份伪造与越权
- 移除可伪造的 `x-user-data` 请求头，管理员身份强制 JWT 校验
- `changeQQ` 增加当前密码校验；`getNotificationSettings` 修复 IDOR 越权

### 3. 会话与登录安全
- refreshToken 黑名单机制（登出后刷新令牌失效）
- 登录失败限流与账户锁定（连续失败锁定）
- 验证码与场景（login/register/password）绑定，防止跨场景复用
- 新设备登录检测与站内/邮件安全提醒
- 忘记密码 + 密码重置后，该用户所有旧 token（含其他设备）立即失效

### 4. 输入与注入防护
- 前端存储型 XSS 修复（第三轮安全修复）
- SQL 注入关键词检测中间件（完整注入语句模式，避免误杀）
- 图片上传类型/大小校验加固

### 5. 基础设施与运行安全
- Redis 连接容错与限流失效修复（Redis 不可用时优雅降级）
- 公告接口防刷、Redis 计数键 TTL 防内存无限增长
- IP 访问统计记录设备信息、安全日志审计事件记录

> ⚠️ **需要强调的是：上述补丁并不代表项目是安全的。**
> 补丁覆盖的是"当时被发现的问题"，而早期代码中未被发现的问题、
> 补丁之间的相互影响、新引入的代码，都可能仍然存在安全风险。
> **请务必按本文档第五节建议进行独立安全审计。**

## 四、潜在风险声明

**本项目未经专业安全审计，以下风险需要使用者知悉并自行承担：**

### 1. 安全漏洞风险（最高优先级）
- 早期代码可能包含 SQL 注入、XSS、越权访问、认证绕过、敏感信息泄露等
  常见 Web 安全漏洞
- 第三方依赖（npm 包）未经过系统性的漏洞扫描与版本治理
- 建议部署前使用安全扫描工具（如 OWASP ZAP、npm audit、Snyk 等）进行审计

### 2. 功能缺陷风险
- 可能存在逻辑错误、边界条件处理不当、并发问题、数据一致性问题等
- 部分功能可能只在特定场景下正常，未覆盖全部边界情况

### 3. 代码质量风险
- 早期代码缺乏统一的编码规范，可能存在重复代码、冗余逻辑、命名混乱
- 错误处理可能不完整，异常路径可能导致服务异常或数据异常

### 4. 数据安全风险
- 系统包含用户个人信息（邮箱、QQ号、学校班级等），若存在漏洞可能导致
  数据泄露
- 生产环境务必启用 MongoDB / Redis 的认证与访问控制，切勿使用默认配置

### 5. 依赖与兼容性风险
- 依赖版本可能未锁定或存在过时组件
- 环境差异（Node.js / MongoDB / Redis 版本）可能导致未预期行为

## 五、使用与部署建议

如果您决定使用或部署本项目，强烈建议：

1. **生产环境谨慎使用**：本项目更适合学习、教学或内部小范围使用，不推荐
   直接用于对安全性要求高的生产环境，除非经过完整的安全审计
2. **部署前安全审计**：使用自动化扫描工具 + 人工代码审查，重点关注认证、
   授权、输入校验、敏感信息处理等模块
3. **启用依赖扫描**：定期执行 `npm audit`，及时修复高危依赖
4. **强化基础设施安全**：
   - 为 MongoDB / Redis 设置强密码与访问控制（仅监听内网/本机）
   - 配置防火墙，仅开放必要端口
   - 生产环境使用 HTTPS
5. **数据备份**：定期备份 MongoDB 数据，并验证备份可恢复性
6. **关注更新**：关注本仓库的提交记录与 issue，及时获取问题修复

## 六、已知问题

> 此部分用于记录已被发现的已知问题，欢迎通过 issue 反馈。

- （暂无记录 —— 未知问题可能更多，请在使用中留意并反馈）

## 七、贡献与反馈

- 本项目开源，欢迎提交 issue 反馈 Bug、漏洞与改进建议
- 提交安全漏洞时，建议遵循负责任披露原则，先与维护者私下沟通
- 本项目代码由 AI 生成，**任何形式的代码审查与修复贡献都极为有价值**

## 八、免责声明

**使用本软件即表示您已阅读、理解并同意以下条款：**

1. **按"现状"提供（AS IS）**：本软件按原样提供，不附带任何形式的明示或
   暗示担保，包括但不限于适销性、特定用途适用性、不侵权的担保。

2. **使用风险自负**：您应自行承担使用本软件的全部风险。软件的质量、可用性、
   正确性与安全性均不作任何保证。

3. **责任限制**：在任何情况下，维护者均不对因使用或无法使用本软件而产生的
   任何直接、间接、偶然、特殊或后果性损害负责，包括但不限于数据丢失、
   数据泄露、业务中断、利润损失、声誉损失，即使维护者已被告知发生此类
   损害的可能性。

4. **未经审查代码免责**：本项目未经专业安全审计与代码审查。因使用（含部署、
   二次开发、修改后版本）本软件而遭受的任何损失，包括但不限于因安全漏洞
   导致的入侵、数据泄露、账号被盗、财产损失，维护者均不承担任何责任。

5. **环境与第三方免责**：因运行环境（操作系统、服务器、数据库、网络）、
   第三方组件（npm 依赖、MongoDB、Redis 等）或使用者自身操作不当（如未按
   本文档建议配置、未启用访问控制、未备份数据）造成的损失，与维护者无关。

6. **修改版本免责**：任何修改、分发、二次开发后的版本，其行为与后果由
   修改者自行负责，与原始维护者无关。

7. **无安全承诺**：本文档中"已实施的安全加固"一节仅为客观事实陈述，
   不构成任何形式的安全保证、担保或承诺。

---

*本文档旨在诚实披露项目背景。使用本项目即表示您已知悉并接受上述风险与免责条款。*
